Решения по настройке vault, чтобы не переспрашивать себя через месяц. Заполняется при
адаптации по `INTEGRATION.md` и живёт дальше — сюда же дописываются решения, принятые по
ходу работы.

## Структура
- Дневные логи — `Log/YYYY/MM/`, отчёты — `Log/Reports/`.
- Вложения — только в `files/`, `.md` туда не кладу: каталог исключён из поиска.

## Настройки, выбранные при адаптации
- Название и тип vault:
- День недельного отчёта:
- Маркеры дат и секции `# Week:` / `# Week+`:
- Порог старых задач:
- Внешний трекер:
- Решили не подключать:

## Договорённости с собой
- Задача без даты `➕` считается забытой и разбирается на ближайшем ревью.
